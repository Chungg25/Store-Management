- `[ ]` Update `api/index.py` to remove email and scheduler logic
- `[ ]` Update `get_sheets` in `api/index.py` to handle `type`
- `[ ]` Update `GET /api/items` and `GET /api/transactions` to support `type`
- `[ ]` Update `POST /api/transaction` with dual-sync logic (update quantity and log history in both sheets if SKU matches)
- `[ ]` Update `PUT /api/update-item` and `PUT /api/update-threshold` with dual-sync logic
- `[ ]` Refactor `Inventory` component in `src/App.jsx` to use `type` and `title` props
- `[ ]` Update all `fetch` requests in `Inventory` to pass `type`
- `[ ]` Update `App.jsx` routing and Sidebar menu for "Tổng kho" and "Vật tư quan trọng"
- `[ ]` Test backend endpoints with dual-sync
- `[ ]` Verify UI updates
